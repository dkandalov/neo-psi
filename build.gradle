apply plugin: 'java'

repositories {
	mavenCentral()
}

dependencies {
	compile group: 'org.neo4j', name: 'neo4j', version: '2.0.0-M06'
}

task downloadNeo4jLibs(type: Copy) {
	from configurations.runtime
	into './lib'
}